<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.planb.dao.AdminActionMapper">
    <insert id="insert" parameterType="com.planb.entity.AdminAction">
        <!-- 新增管理员操作记录 -->
        INSERT INTO adminactions (action_date, action_type, book_id, admin_user_id)
        VALUES (#{actionDate}, #{actionType}, #{bookId}, #{adminUserId})
    </insert>
</mapper>